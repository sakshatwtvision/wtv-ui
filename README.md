## Todos

1. Like other semantic color variables we have, check if we can include a "muted" variant which will mimic the gray colors.
2. Internally, none of the components should reference the color variables, they should only reference the semantic variables.
3. The `cn` classes should be ordered correctly. For example, all margin related classes for md: and lg: should be on one line,
4. md: and lg: should be changed to medium: and large: respectively
